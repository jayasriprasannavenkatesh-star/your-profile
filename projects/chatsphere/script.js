const messages=document.querySelector('#messages');document.querySelector('#composer').addEventListener('submit',e=>{e.preventDefault();const input=document.querySelector('#message');const text=input.value.trim();if(!text)return;const p=document.createElement('p');p.className='sent';p.textContent=text;messages.appendChild(p);input.value='';messages.scrollTop=messages.scrollHeight});document.querySelector('#search').addEventListener('input',e=>{const q=e.target.value.toLowerCase();document.querySelectorAll('.person').forEach(x=>x.hidden=!x.dataset.name.toLowerCase().includes(q))});